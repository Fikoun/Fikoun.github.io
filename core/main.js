function main()
{
    this.addObject('isoback')
        .setPosition(-100, -100)

    this.addObject('isoback')
        .setPosition(100, 610)
        .setDraggable();

    this.addObject('isoback')
        .setPosition(300, 610)
        .setAngle(90)
        .setDraggable();

}